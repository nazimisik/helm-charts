{{- define "deployment.processor.k8sattributes" -}}
otelcol.processor.k8sattributes "default" {
  // https://grafana.com/docs/agent/latest/flow/reference/components/otelcol.processor.k8sattributes/
  extract {
    metadata = [
      "k8s.namespace.name",
      "k8s.pod.name",
      "k8s.deployment.name",
      "k8s.statefulset.name",
      "k8s.daemonset.name",
      "k8s.cronjob.name",
      "k8s.job.name",
      "k8s.node.name",
      "k8s.pod.uid",
      "k8s.pod.start_time",
    ]
  }

  pod_association {
    source {
      from = "connection"
    }
  }

  output {
    logs   = [otelcol.processor.transform.add_k8s_attributes.input]
    traces = [otelcol.processor.transform.add_k8s_attributes.input]
  }
}

{{ end }}

